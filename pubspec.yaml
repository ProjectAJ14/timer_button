name: timer_button
description: A button that starts a timer and disables itself until the timer is done.
version: 2.0.1
homepage: https://github.com/ProjectAJ14/timer_button
documentation: https://github.com/ProjectAJ14/timer_button
repository: https://github.com/ProjectAJ14/timer_button
issue_tracker: https://github.com/ProjectAJ14/timer_button/issues

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
flutter:
